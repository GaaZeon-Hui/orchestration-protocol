将 Claude Code 的会话转录同步到 SQLite，用于分析 token 使用、工具调用、团队协作数据
bash指令
# 首次同步所有会话历史
npx ccrecall sync

# 查看统计数据
npx ccrecall stats

# 搜索特定话题
npx ccrecall search "lock acquisition failed"

# 自定义 SQL 查询
npx ccrecall query "SELECT session_id, input_tokens, output_tokens FROM messages WHERE content_text LIKE '%atomic-lock%'"

用来：

审计哪个 Agent 消耗 token 最多

追溯"锁获取失败"问题的历史会话

分析 Orchestrator 审批效率